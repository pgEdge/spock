results
regression_output
tmp_check
/utils/spock_create_subscriber/spock_create_subscriber
/utils/spock_create_subscriber/.deps/
.vimrc
*.o
*.so
*.bc
*.gcda
*.gcno
*~
/postgres
spock.control
tags
.vscode
tmp/

# Single-PG18 installcheck test rig (tests/run-single-pg18-installcheck.sh).
# Contains the PG clone, build, install, PGDATA dirs and logs; can grow to
# several gigabytes.  Always ignored -- re-create by running the script.
# Deliberately not dot-prefixed so the directory is visible in Finder / ls
# and easy to inspect.
single-pg18-installcheck/

# Cluster-upgrade test rig (tests/run-cluster-upgrade.sh).  Holds a PG clone
# plus install per version under test, two Spock source trees, a PGDATA dir per
# node per version, logs and reports; tens of gigabytes.  Always ignored --
# re-create by running the script.
cluster-upgrade/
